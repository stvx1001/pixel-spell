# Pixel Spell

The website of Pixel Spell, a design & engineering studio in Jakarta.

Built with Next.js, Tailwind CSS and TypeScript from the Figma design.

```bash
npm install
npm run dev
```

Open http://localhost:3000.

Character art goes in `public/characters/` — see `CLAUDE.md` for the file names.
